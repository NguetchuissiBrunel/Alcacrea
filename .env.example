# URL du backend MedicalBackend (sans suffixe /api)
VITE_API_URL=https://parser.datapipe.duckdns.org

# Mettre à false quand le backend est prêt
VITE_USE_MOCK=true

# Endpoints auth attendus (quand VITE_USE_MOCK=false) :
# POST /auth/login, POST /auth/register, GET /auth/me, PATCH /auth/me, POST /auth/logout
