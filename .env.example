# Copier ce fichier en .env à la racine de frontend/
#
# En dev : laisser vide → requêtes /api proxifiées par Vite vers le backend
# En prod : URL complète du backend (sans suffixe /api)
VITE_API_URL=
